---
interface Props {
  class?: string;
}

const { class: customClass = "" } = Astro.props as Props;
---

<h2
  class={`bg-gradient-to-r from-white via-gray-400 to-white bg-clip-text text-center text-transparent heading-h2 w-fit mx-auto py-2 ${customClass}`}
>
  <slot />
</h2>
