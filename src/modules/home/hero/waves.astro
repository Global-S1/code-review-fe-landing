<div
  class="wave-background -z-10 absolute left-0 top-0 w-full h-full overflow-hidden"
>
  <div class="fondo-codari"></div>
</div>

<style>
  .fondo-codari {
    position: relative;
    width: 150%;
    height: 150%;
    border-radius: 2rem;
    overflow: hidden;
    background-color: #000;
    background: radial-gradient(
        ellipse at 20% 40%,
        color-mix(in oklab, var(--color-primary) 40%, black) 0%,
        transparent 60%
      ),
      radial-gradient(
        ellipse at 80% 60%,
        color-mix(in oklab, var(--color-primary) 60%, black) 0%,
        transparent 50%
      ),
      linear-gradient(
        135deg,
        color-mix(in oklab, var(--color-primary) 24%, black) 0%,
        #000000 100%
      );
    background-blend-mode: overlay;
  }
  .fondo-codari::before {
    content: "";
    position: absolute;
    top: -20%;
    left: -20%;
    width: 150%;
    height: 150%;
    background: radial-gradient(
      circle,
      color-mix(in oklab, var(--color-primary) 40%, black),
      transparent 70%
    );
    animation: movimientoAurora 3s ease-in-out infinite alternate;
    z-index: 0;
  }
  @keyframes movimientoAurora {
    0% {
      transform: rotate(0deg) translateX(0);
      opacity: 0.5;
    }
    100% {
      transform: rotate(20deg) translateX(10%);
      opacity: 0.9;
    }
  }
  .fondo-codari .contenido {
    position: relative;
    z-index: 1;
    color: white;
    text-align: center;
    padding: 2rem;
  }
  .fondo-codari .resaltado {
    color: #00e0ff;
  }
</style>
